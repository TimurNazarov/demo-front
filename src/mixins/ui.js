export default {
	methods: {
		// toggle_elements_class(elements, class_name, remove = false) {
		// 	let list = document.querySelectorAll(elements)
		// 	console.log(list)
  //        for (var i = 0; i < list.length; ++i) {
  //        	if(!remove) {
  //              list[i].classList.add(class_name)
  //        	} else {
  //              list[i].classList.remove(class_name)
  //        	}
  //        }
		// }
	}
}